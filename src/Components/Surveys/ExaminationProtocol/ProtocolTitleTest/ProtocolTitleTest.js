import React from 'react';

const ProtocolTitleTest = (props) => {

    const {
        children
    } = props

    return (
        <p className='protocol__qu-test'>
            {children}
        </p>
    );
}

export default ProtocolTitleTest;