import React from 'react';

const ProtocolSubtitleThree = (props) => {

    const {
        children
    } = props

    return (
        <p className='protocol__sub3title-block'>
            {children}
        </p>
    );
};

export default ProtocolSubtitleThree;